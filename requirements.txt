# """
# Utilizar un interprete de python 3.8
# Pasos para el uso del sistema (commit 163):
#    JS                                                                            
#   npm install                                                                                                                                                      
                                                                                                                                                                   
#    Python (en un venv Python 3.8)
#   python -m venv .venv                                                                                                                                             
#   .venv\Scripts\activate                                                                                                                                         
#   pip install -r requirements.txt

#   Y para correr:
#    Terminal 1 — backend
#   uvicorn api.mainAPI:app --host 127.0.0.1 --port 8000

#    Terminal 2 — frontend
#   npm start
# """

# NOTA: dependencias DIRECTAS con versiones FIJAS, probadas en Python 3.8.10 y
# CROSS-PLATFORM (instalables tanto en Linux como en Windows). Esta es la via
# de instalacion recomendada en AMBOS sistemas:
#     pip install -r requirements.txt
# requirements.lock.txt es el cierre transitivo exacto, pero SOLO sirve en
# Windows (ver su cabecera). En Linux usar este archivo.

# ── Core API ──────────────────────────────
fastapi[standard]==0.115.8
uvicorn==0.33.0
websockets==13.1

# ── Inferencia ────────────────────────────
tensorflow==2.12.0
# onnxruntime-gpu trae el Execution Provider de CUDA: es el runtime usado en
# Linux con GPU (CUDA 11.8). En Windows tambien funciona; en CPU cae al
# CPUExecutionProvider sin romper. Para CUDA 12.x usar onnxruntime-gpu==1.18.x.
onnxruntime-gpu==1.17.1
torch==2.4.1

# ── Procesamiento de imagenes ─────────────
Pillow==10.4.0
numpy==1.23.5
opencv-python==4.11.0.86
python-multipart==0.0.20

# ── Entrenamiento YOLOv7 (no necesario para inferencia) ──
# matplotlib>=3.2.2
# PyYAML>=5.3.1
# requests>=2.23.0
# scipy>=1.4.1
# torchvision>=0.8.1
# tqdm>=4.41.0
# protobuf<4.21.3
# pandas